//trójkąt o podanym rozmiarze z gwiazdek

#include <stdio.h>
#include <limits.h>
#include <stdint.h>
#include <float.h>

int main(void) {
    // char c;
    // scanf("%c", &c);

    // int size;
    // scanf("%d", &size);
    // char letter;
    // scanf("%c", &letter);
    // int row;
    // scanf("%d", &row);   

    // printf("%c", letter);

    int a,b;
    scanf("%d", &a);
    scanf("%d", &b);

    printf("%d %d", a, b);
}